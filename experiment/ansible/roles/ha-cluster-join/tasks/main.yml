- name: HA cluster join csync2
  shell: ha-cluster-join -y -c {{ db0_ip }} csync2

- name: HA cluster join cluster
  shell: ha-cluster-join -y cluster

- name: cib status
  shell: crm cib cibstatus show
  register: cib_status
- name: cib status debug
  debug:
    var: cib_status
